{
  "name": "nextup-api",
  "version": "1.0.0",
  "private": true,
  "description": "Patient-facing API for NextUp — predictive OPD queue and departure times",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server.js",
    "deploy": "npx vercel --prod"
  }
}
