# Where the enquiry form POSTs its JSON payload.
# Leave unset and the form still validates and shows its states, but nothing is
# sent anywhere (a warning is logged in dev). The WhatsApp path always works.
VITE_ENQUIRY_ENDPOINT=
